<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFE146"
    tools:context=".view.splash.SplashActivity">

    <TextView
        android:id="@+id/app_icon_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pretendard_semi_bold"
        android:text="B"
        android:textColor="@color/black"
        android:layout_marginStart="40dp"
        android:layout_marginTop="164dp"
        android:textSize="82dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/app_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pretendard_semi_bold"
        android:text="비맵"
        android:textColor="@color/black"
        android:textSize="30dp"
        android:layout_marginTop="16.7dp"
        app:layout_constraintLeft_toLeftOf="@+id/app_icon_tv"
        app:layout_constraintTop_toBottomOf="@+id/app_icon_tv"/>

    <TextView
        android:id="@+id/app_desc_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pretendard_regular"
        android:text="Become their map"
        android:textColor="#80000000"
        android:textSize="20dp"
        android:layout_marginTop="16.7dp"
        app:layout_constraintLeft_toLeftOf="@+id/app_icon_tv"
        app:layout_constraintTop_toBottomOf="@+id/app_title_tv"/>
</androidx.constraintlayout.widget.ConstraintLayout>